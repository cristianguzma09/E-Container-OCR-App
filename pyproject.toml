[project]
name = "econtainer-ocr-app"
version = "1.0.0"
description = "Backend service for detecting and reading maritime container IDs using AI/OCR."
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
asyncio_mode = "auto"
